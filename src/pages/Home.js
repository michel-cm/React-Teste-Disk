import '../styles/Dashboard.css'

import { Header } from '../components/Header';

export const Home = () => {
    return (
        <div className=''>
            <Header />
            Tela Home, instruções
        </div>
    );
}

